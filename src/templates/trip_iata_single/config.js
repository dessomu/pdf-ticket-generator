const config = {
  id: "trip_iata_single",
  label: "Trip IATA (Single)",
  maxPassengers: 1,
  defaults: {
      // No specific defaults requested
  },
  barcode: {
    enabled: false,
  },
};

export default config;
